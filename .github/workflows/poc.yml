name: poc-test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Claude Action (PoC)
        run: |
          echo "=== START DEBUG ==="
          echo "Dumping environment variables..."
          env

          echo "=== GITHUB CONTEXT ==="
          echo "$GITHUB_CONTEXT"

          echo "=== FILE SYSTEM ==="
          ls -la

          echo "=== TRY READING SECRETS ==="
          echo $ANTHROPIC_API_KEY

          echo "=== DONE ==="
